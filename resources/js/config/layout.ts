// ─────────────────────────────────────────────────────────────────────────────
// Layout sizing constants
// Edit values here — all sidebar + main-area measurements reference this file.
// ─────────────────────────────────────────────────────────────────────────────

/** Sidebar lebar penuh (expanded) dalam px */
export const SIDEBAR_W_EXPANDED  = 272;

/** Sidebar icon-only (collapsed) dalam px */
export const SIDEBAR_W_COLLAPSED = 72;

/** Tinggi topbar dalam px */
export const TOPBAR_HEIGHT = 56;

// ── Responsive breakpoints (px) ───────────────────────────────────────────────

/** Di bawah nilai ini: sidebar menjadi drawer overlay, main area full-width */
export const BREAKPOINT_MOBILE = 768;

/** Di bawah nilai ini: sidebar auto-collapse ke icon-only */
export const BREAKPOINT_TABLET = 1024;
